# [tool.towncrier]
#     directory = 'changes'

[build-system]
    requires = ['setuptools>=42', 'wheel', 'setuptools_scm[toml]>=3.4']

[tool.setuptools_scm]
    write_to = 'numjuggler/version.py'

[dependency-groups]
test = [
    "pytest>=9.0.3",
]


